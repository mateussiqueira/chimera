name: configuration
description: Configure urls, environments and other app options.
version: 1.1.0
publish_to: none
homepage: https://grupoq.io/

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  iterable:
    git:
      url: https://github.com/qcx/chimera.git
      ref: iterable-v1.2.0
      path: commons/iterable

dev_dependencies:
  flutter_lints: ^2.0.1
  flutter_test:
    sdk: flutter

flutter: